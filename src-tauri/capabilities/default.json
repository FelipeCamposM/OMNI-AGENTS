{
  "identifier": "default",
  "description": "Default capability for OMNI AGENTS",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
